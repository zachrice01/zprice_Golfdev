Deploy static content to Pages (disabled)
name: Deploy static content to Pages (disabled)

on: workflow_dispatch:

This workflow was disabled (no automatic push trigger) to avoid
overwriting the built site. ci.yml is used to build and deploy the
site from quartz/public.
permissions: contents: read pages: write id-token: write

jobs: noop: runs-on: ubuntu-latest steps: - name: Disabled run: echo "static.yml disabled; use ci.yml for deploy"
